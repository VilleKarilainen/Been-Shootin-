using UnityEngine;

public class CanSpawn : MonoBehaviour 
{
  /*public GameObject canPrefab;
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {
            GameObject newCan = Instantiate(canPrefab, transform.position, Quaternion.identity);
            Rigidbody2D rb = newCan.GetComponent<Rigidbody2D>();
            // Give it an upward and sideways "kick"
            rb.AddForce(new Vector2(Random.Range(2f, 5f), 10f), ForceMode2D.Impulse);
            //Instantiate(canPrefab, transform.position, Quaternion.identity);
        }
    }*/
}
